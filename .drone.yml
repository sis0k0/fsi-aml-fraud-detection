# FSI Fraud Detection - Unified Pod Deployment
# Deploys frontend + fraud backend + AML backend as 3 containers in same pod

kind: pipeline
type: kubernetes
name: staging

trigger:
  branch:
    - staging

steps:
  # Step 1: Build Frontend Image
  - name: publish-frontend
    image: plugins/kaniko-ecr
    settings:
      create_repository: true
      registry: 795250896452.dkr.ecr.us-east-1.amazonaws.com
      repo: industrysolutions/fsi-fraud-detection-frontend
      tags:
        - git-${DRONE_COMMIT_SHA:0:7}
        - latest
      dockerfile: docker/Dockerfile.frontend
      access_key:
        from_secret: ecr_access_key
      secret_key:
        from_secret: ecr_secret_key
      build_args:
        - NEXT_PUBLIC_API_URL=/api/fraud
        - NEXT_PUBLIC_AML_API_URL=/api/aml
        - BIAN_MODEL_URL=https://leafy-bank-bian-model.industrysolutions.staging.corp.mongodb.com
    when:
      event:
        - push

  # Step 2: Build Fraud Backend Image
  - name: publish-backend
    image: plugins/kaniko-ecr
    settings:
      create_repository: true
      registry: 795250896452.dkr.ecr.us-east-1.amazonaws.com
      repo: industrysolutions/fsi-fraud-detection-backend
      tags:
        - git-${DRONE_COMMIT_SHA:0:7}
        - latest
      dockerfile: Dockerfile.backend
      access_key:
        from_secret: ecr_access_key
      secret_key:
        from_secret: ecr_secret_key
    when:
      event:
        - push

  # Step 3: Build AML Backend Image
  - name: publish-aml-backend
    image: plugins/kaniko-ecr
    settings:
      create_repository: true
      registry: 795250896452.dkr.ecr.us-east-1.amazonaws.com
      repo: industrysolutions/fsi-fraud-detection-aml-backend
      tags:
        - git-${DRONE_COMMIT_SHA:0:7}
        - latest
      dockerfile: Dockerfile.aml-backend
      access_key:
        from_secret: ecr_access_key
      secret_key:
        from_secret: ecr_secret_key
    when:
      event:
        - push

  # Step 4: Deploy Combined Pod to Staging
  - name: deploy-staging
    image: public.ecr.aws/kanopy/drone-helm:v3
    settings:
      chart: mongodb/web-app
      chart_version: 4.30.0
      add_repos: [mongodb=https://10gen.github.io/helm-charts]
      namespace: industrysolutions
      release: fsi-fraud-detection
      values:
        # Frontend main container
        - 'image.repository=795250896452.dkr.ecr.us-east-1.amazonaws.com/industrysolutions/fsi-fraud-detection-frontend'
        - 'image.tag=git-${DRONE_COMMIT_SHA:0:7}'
        # Ingress configuration
        - 'ingress.enabled=true'
        - 'ingress.hosts[0]=fsi-fraud-detection.industrysolutions.staging.corp.mongodb.com'
        # WebSocket support via nginx ingress
        - 'ingress.annotations.nginx\.ingress\.kubernetes\.io/proxy-read-timeout="3600"'
        - 'ingress.annotations.nginx\.ingress\.kubernetes\.io/proxy-send-timeout="3600"'
        # Service mesh
        - 'mesh.enabled=true'
        # Resource limits for frontend
        - 'resources.limits.cpu=500m'
        - 'resources.limits.memory=600Mi'
        - 'resources.requests.cpu=100m'
        - 'resources.requests.memory=150Mi'
        # Sidecar: Fraud Backend
        - 'sidecarContainers[0].name=fraud-backend'
        - 'sidecarContainers[0].image=795250896452.dkr.ecr.us-east-1.amazonaws.com/industrysolutions/fsi-fraud-detection-backend:git-${DRONE_COMMIT_SHA:0:7}'
        - 'sidecarContainers[0].ports[0].containerPort=8000'
        - 'sidecarContainers[0].ports[0].name=fraud-api'
        - 'sidecarContainers[0].resources.limits.cpu=350m'
        - 'sidecarContainers[0].resources.limits.memory=750Mi'
        - 'sidecarContainers[0].resources.requests.cpu=100m'
        - 'sidecarContainers[0].resources.requests.memory=256Mi'
        # Sidecar: AML Backend
        - 'sidecarContainers[1].name=aml-backend'
        - 'sidecarContainers[1].image=795250896452.dkr.ecr.us-east-1.amazonaws.com/industrysolutions/fsi-fraud-detection-aml-backend:git-${DRONE_COMMIT_SHA:0:7}'
        - 'sidecarContainers[1].ports[0].containerPort=8001'
        - 'sidecarContainers[1].ports[0].name=aml-api'
        - 'sidecarContainers[1].resources.limits.cpu=350m'
        - 'sidecarContainers[1].resources.limits.memory=750Mi'
        - 'sidecarContainers[1].resources.requests.cpu=100m'
        - 'sidecarContainers[1].resources.requests.memory=256Mi'
      api_server: https://api.staging.corp.mongodb.com
      kubernetes_token:
        from_secret: staging_kubernetes_token
      values_files: ['environment/staging-combined.yaml']
    when:
      event:
        - push

---
kind: pipeline
type: kubernetes
name: production

trigger:
  branch:
    - main

steps:
  # Step 1: Build Frontend Image
  - name: publish-frontend-prod
    image: plugins/kaniko-ecr
    settings:
      create_repository: true
      registry: 795250896452.dkr.ecr.us-east-1.amazonaws.com
      repo: industrysolutions/fsi-fraud-detection-frontend-prod
      tags:
        - git-${DRONE_COMMIT_SHA:0:7}-prod
        - prod-latest
      dockerfile: docker/Dockerfile.frontend
      access_key:
        from_secret: ecr_access_key
      secret_key:
        from_secret: ecr_secret_key
      build_args:
        - NEXT_PUBLIC_API_URL=/api/fraud
        - NEXT_PUBLIC_AML_API_URL=/api/aml
        - BIAN_MODEL_URL=https://leafy-bank-bian-model.industrysolutions.prod.corp.mongodb.com
    when:
      event:
        - push

  # Step 2: Build Fraud Backend Image
  - name: publish-backend-prod
    image: plugins/kaniko-ecr
    settings:
      create_repository: true
      registry: 795250896452.dkr.ecr.us-east-1.amazonaws.com
      repo: industrysolutions/fsi-fraud-detection-backend-prod
      tags:
        - git-${DRONE_COMMIT_SHA:0:7}-prod
        - prod-latest
      dockerfile: Dockerfile.backend
      access_key:
        from_secret: ecr_access_key
      secret_key:
        from_secret: ecr_secret_key
    when:
      event:
        - push

  # Step 3: Build AML Backend Image
  - name: publish-aml-backend-prod
    image: plugins/kaniko-ecr
    settings:
      create_repository: true
      registry: 795250896452.dkr.ecr.us-east-1.amazonaws.com
      repo: industrysolutions/fsi-fraud-detection-aml-backend-prod
      tags:
        - git-${DRONE_COMMIT_SHA:0:7}-prod
        - prod-latest
      dockerfile: Dockerfile.aml-backend
      access_key:
        from_secret: ecr_access_key
      secret_key:
        from_secret: ecr_secret_key
    when:
      event:
        - push

  # Step 4: Deploy Combined Pod to Production
  - name: deploy-production
    image: public.ecr.aws/kanopy/drone-helm:v3
    settings:
      chart: mongodb/web-app
      chart_version: 4.30.0
      add_repos: [mongodb=https://10gen.github.io/helm-charts]
      namespace: industrysolutions
      release: fsi-fraud-detection-prod
      values:
        # Frontend main container
        - 'image.repository=795250896452.dkr.ecr.us-east-1.amazonaws.com/industrysolutions/fsi-fraud-detection-frontend-prod'
        - 'image.tag=git-${DRONE_COMMIT_SHA:0:7}-prod'
        # Ingress configuration
        - 'ingress.enabled=true'
        - 'ingress.hosts[0]=fsi-fraud-detection.industrysolutions.prod.corp.mongodb.com'
        # WebSocket support via nginx ingress
        - 'ingress.annotations.nginx\.ingress\.kubernetes\.io/proxy-read-timeout="3600"'
        - 'ingress.annotations.nginx\.ingress\.kubernetes\.io/proxy-send-timeout="3600"'
        # Service mesh
        - 'mesh.enabled=true'
        # Resource limits for frontend
        - 'resources.limits.cpu=500m'
        - 'resources.limits.memory=600Mi'
        - 'resources.requests.cpu=100m'
        - 'resources.requests.memory=150Mi'
        # Sidecar: Fraud Backend
        - 'sidecarContainers[0].name=fraud-backend'
        - 'sidecarContainers[0].image=795250896452.dkr.ecr.us-east-1.amazonaws.com/industrysolutions/fsi-fraud-detection-backend-prod:git-${DRONE_COMMIT_SHA:0:7}-prod'
        - 'sidecarContainers[0].ports[0].containerPort=8000'
        - 'sidecarContainers[0].ports[0].name=fraud-api'
        - 'sidecarContainers[0].resources.limits.cpu=350m'
        - 'sidecarContainers[0].resources.limits.memory=750Mi'
        - 'sidecarContainers[0].resources.requests.cpu=100m'
        - 'sidecarContainers[0].resources.requests.memory=256Mi'
        # Sidecar: AML Backend
        - 'sidecarContainers[1].name=aml-backend'
        - 'sidecarContainers[1].image=795250896452.dkr.ecr.us-east-1.amazonaws.com/industrysolutions/fsi-fraud-detection-aml-backend-prod:git-${DRONE_COMMIT_SHA:0:7}-prod'
        - 'sidecarContainers[1].ports[0].containerPort=8001'
        - 'sidecarContainers[1].ports[0].name=aml-api'
        - 'sidecarContainers[1].resources.limits.cpu=350m'
        - 'sidecarContainers[1].resources.limits.memory=750Mi'
        - 'sidecarContainers[1].resources.requests.cpu=100m'
        - 'sidecarContainers[1].resources.requests.memory=256Mi'
      api_server: https://api.prod.corp.mongodb.com
      kubernetes_token:
        from_secret: prod_kubernetes_token
      values_files: ['environment/production-combined.yaml']
    when:
      event:
        - push

---
# ============================================================================
# TEMPORARY blue/green pipeline — DELETE after the ist-prod cutover completes.
#
# Deploys the SAME code as `staging` to the PROD cluster under a parallel
# release + host, so it can be verified before `main` is touched. Triggered on
# the throwaway `prod-green` branch ONLY — it can never fire from main/staging.
#
#   green: fsi-fraud-detection-bian-prod  -> ist-prod   (secret fsi-fraud-detection-bian)
#   blue:  fsi-fraud-detection-prod       -> ist-shared (secret fsi-fraud-detection-secrets)
#
# Runbook: .claude/outputs/prod-cutover-fsi-fraud-detection-bian.md
# Removed in Phase 3, when the `production` pipeline above is repointed here.
# ============================================================================
kind: pipeline
type: kubernetes
name: production-bian

trigger:
  branch:
    - prod-green

steps:
  # Step 1: Build Frontend Image
  - name: publish-frontend-bian
    image: plugins/kaniko-ecr
    settings:
      create_repository: true
      registry: 795250896452.dkr.ecr.us-east-1.amazonaws.com
      repo: industrysolutions/fsi-fraud-detection-bian-frontend
      tags:
        - git-${DRONE_COMMIT_SHA:0:7}-prod
        - prod-latest
      dockerfile: docker/Dockerfile.frontend
      access_key:
        from_secret: ecr_access_key
      secret_key:
        from_secret: ecr_secret_key
      build_args:
        - NEXT_PUBLIC_API_URL=/api/fraud
        - NEXT_PUBLIC_AML_API_URL=/api/aml
        - BIAN_MODEL_URL=https://leafy-bank-bian-model.industrysolutions.prod.corp.mongodb.com
    when:
      event:
        - push

  # Step 2: Build Fraud Backend Image
  - name: publish-backend-bian
    image: plugins/kaniko-ecr
    settings:
      create_repository: true
      registry: 795250896452.dkr.ecr.us-east-1.amazonaws.com
      repo: industrysolutions/fsi-fraud-detection-bian-backend
      tags:
        - git-${DRONE_COMMIT_SHA:0:7}-prod
        - prod-latest
      dockerfile: Dockerfile.backend
      access_key:
        from_secret: ecr_access_key
      secret_key:
        from_secret: ecr_secret_key
    when:
      event:
        - push

  # Step 3: Build AML Backend Image
  - name: publish-aml-backend-bian
    image: plugins/kaniko-ecr
    settings:
      create_repository: true
      registry: 795250896452.dkr.ecr.us-east-1.amazonaws.com
      repo: industrysolutions/fsi-fraud-detection-bian-aml-backend
      tags:
        - git-${DRONE_COMMIT_SHA:0:7}-prod
        - prod-latest
      dockerfile: Dockerfile.aml-backend
      access_key:
        from_secret: ecr_access_key
      secret_key:
        from_secret: ecr_secret_key
    when:
      event:
        - push

  # Step 4: Deploy Combined Pod to the ist-prod green link
  - name: deploy-production-bian
    image: public.ecr.aws/kanopy/drone-helm:v3
    settings:
      chart: mongodb/web-app
      chart_version: 4.30.0
      add_repos: [mongodb=https://10gen.github.io/helm-charts]
      namespace: industrysolutions
      release: fsi-fraud-detection-bian-prod
      values:
        # Frontend main container
        - 'image.repository=795250896452.dkr.ecr.us-east-1.amazonaws.com/industrysolutions/fsi-fraud-detection-bian-frontend'
        - 'image.tag=git-${DRONE_COMMIT_SHA:0:7}-prod'
        # Ingress configuration
        - 'ingress.enabled=true'
        - 'ingress.hosts[0]=fsi-fraud-detection-bian.industrysolutions.prod.corp.mongodb.com'
        # WebSocket support via nginx ingress
        - 'ingress.annotations.nginx\.ingress\.kubernetes\.io/proxy-read-timeout="3600"'
        - 'ingress.annotations.nginx\.ingress\.kubernetes\.io/proxy-send-timeout="3600"'
        # Service mesh
        - 'mesh.enabled=true'
        # Resource limits for frontend
        - 'resources.limits.cpu=500m'
        - 'resources.limits.memory=600Mi'
        - 'resources.requests.cpu=100m'
        - 'resources.requests.memory=150Mi'
        # Sidecar: Fraud Backend
        - 'sidecarContainers[0].name=fraud-backend'
        - 'sidecarContainers[0].image=795250896452.dkr.ecr.us-east-1.amazonaws.com/industrysolutions/fsi-fraud-detection-bian-backend:git-${DRONE_COMMIT_SHA:0:7}-prod'
        - 'sidecarContainers[0].ports[0].containerPort=8000'
        - 'sidecarContainers[0].ports[0].name=fraud-api'
        - 'sidecarContainers[0].resources.limits.cpu=350m'
        - 'sidecarContainers[0].resources.limits.memory=750Mi'
        - 'sidecarContainers[0].resources.requests.cpu=100m'
        - 'sidecarContainers[0].resources.requests.memory=256Mi'
        # Sidecar: AML Backend
        - 'sidecarContainers[1].name=aml-backend'
        - 'sidecarContainers[1].image=795250896452.dkr.ecr.us-east-1.amazonaws.com/industrysolutions/fsi-fraud-detection-bian-aml-backend:git-${DRONE_COMMIT_SHA:0:7}-prod'
        - 'sidecarContainers[1].ports[0].containerPort=8001'
        - 'sidecarContainers[1].ports[0].name=aml-api'
        - 'sidecarContainers[1].resources.limits.cpu=350m'
        - 'sidecarContainers[1].resources.limits.memory=750Mi'
        - 'sidecarContainers[1].resources.requests.cpu=100m'
        - 'sidecarContainers[1].resources.requests.memory=256Mi'
      api_server: https://api.prod.corp.mongodb.com
      kubernetes_token:
        from_secret: prod_kubernetes_token
      values_files: ['environment/production-bian.yaml']
    when:
      event:
        - push
